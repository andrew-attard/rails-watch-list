<h1>Lists</h1>
<% @lists.all.each do |list| %>
<ul>
  <li><%= link_to list.name, list_path(list), class: 'text-decoration-none' %>
</ul>
<% end %>
<%= link_to "Add a List", new_list_path, class: 'btn-light opacity-25 text-decoration-none' %>
